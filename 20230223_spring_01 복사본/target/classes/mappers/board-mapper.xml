<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="board">

	<insert id="insert">
		insert into board values (
		(select NVL(MAX(BOARD_NUM), 0)+1 from board)
		, #{boardTitle}
		, #{boardWriter}
		, #{boardContent}    
		, #{boardOriginalFilename}
		, #{boardRenameFilename}
		, default
		, DEFAULT 
		, (select NVL(MAX(BOARD_NUM), 0)+1 from board)
		, DEFAULT
		, DEFAULT )
	</insert>
	
	<update id="update">
		update board set
		board_Title = #{boardTitle}
		, board_Content = #{boardContent}
		, board_Original_Filename = #{boardOriginalFilename}
		, board_Rename_Filename = #{boardRenameFilename}
		, board_Date = SYSDATE
		where board_Num = #{boardNum}
	</update>
	
	<delete id="delete">
		delete from board where board_Num = #{boardNum}
	</delete>
	
	<select id="selectOne" resultType="kh.spring.s01.board.model.vo.BoardVo">
		select * from board where board_Num = #{boardNum}
	</select>
	
	<select id="selectList" resultType="kh.spring.s01.board.model.vo.BoardVo">
		select * from board
	</select>
	
	<select id="selectOneCount">
		select count(*) cnt from board
	</select>
</mapper>
